package com.example.LMS.entity.Enum;

public enum ExamStatus {
    CREATED,      // Bài kiểm tra dạng nháp (chỉ Giảng viên thấy)
    PUBLISHED,  // Đã xuất bản (Sinh viên có thể thấy)
    CLOSED      // Đã đóng/Hết giờ làm bài
}
